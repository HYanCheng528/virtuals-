# v1.5.2 更新说明

发布日期：2026-02-14
分支：`release/v1.5.2`

## 本次修改点（相对 v1.5.1）

1. 增加 UI 性能状态条
- 新增 `刷新耗时` 显示（每次刷新完成后显示耗时 ms）。
- 新增 `录入延迟 P50/P95` 显示（基于交易录入延迟列表实时计算）。

2. 交易录入延迟可视化增强
- `时间差(秒)` 列增加颜色分级：
  - `<= 5s`：绿色
  - `<= 15s`：黄色
  - `> 15s`：红色

3. 页面顶部交互优化
- 顶部控制区改为粘性头部（`sticky`），滚动页面时控制区保持可见。
- 表格行新增 hover 高亮，提升读数效率。

4. 项目选择持久化
- 新增本地缓存：刷新网页后自动恢复到上次选择的项目。
- 在以下场景会同步保存当前项目：
  - 手动切换项目
  - 保存/切换项目后
  - 删除项目后回退到可用项目时

5. 默认策略调整
- “仅监控当前项目（停用其它项目监听）”改为默认勾选。

## 影响范围

- 仅前端页面文件：`dashboard.html`
- 不涉及后端交易识别规则变更。
- 不涉及数据库结构变更。
- 不涉及配置字段新增。

## 升级方式

1. 拉取 `release/v1.5.2` 分支代码。
2. 继续使用原有 `config.json` 启动即可。

## 回滚方式

- 需要回滚时可切回 `release/v1.5.1` 分支或 `v1.5.1` tag。
