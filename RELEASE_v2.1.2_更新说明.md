# V2.1.2 更新说明

发布日期：2026-02-15

## 本次更新

1. 新增超速模式（0.25s 刷新）
- 在 UI 顶部新增按钮：`超速模式：关(0.25s)`。
- 支持三档刷新模式：
  - 普通：1s
  - 极速：0.5s
  - 超速：0.25s
- 刷新模式会保存到本地存储（`virtuals_refresh_mode`），刷新页面后保持。
- 兼容旧键 `virtuals_fast_mode`，首次读取后自动迁移。

2. 新增超速保护（自动降级）
- 仅在超速模式下启用保护逻辑。
- 当刷新耗时连续偏高（默认连续 3 次，阈值为 175ms）时，自动降级到极速模式（0.5s），避免页面抖动与请求堆积。

3. 价格展示优化（仅 UI）
- `价格(VIRTUAL/USD)` 现在显示为 4 位小数。
- 鼠标悬停价格时可查看精确原始值（title 提示）。
- 后端计算和入库精度未改动。

4. 版本标识同步
- UI 标题/副标题更新为 `v2.1.2`。
- CLI 帮助描述更新为 `v2.1.2`。
- README 更新为 `v2.1.2` 并新增本版本文档入口。

## 变更文件
- `dashboard.html`
- `virtuals_bot.py`
- `README.md`
- `RELEASE_v2.1.2_更新说明.md`
