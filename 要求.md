# Virtuals 打新/内盘实时扫描机器人（Spec v2）

> 目标：做一个**只读**链上机器人（Base 链），实时抓取 Virtuals 打新/内盘阶段的买入数据，用“手续费 1%”反推该笔花费的 VIRTUAL，并统计：
> - **每笔买入**：买入地址、买到的代币数、手续费、推算总花费 V、成本价、回本 FDV
> - **指定钱包列表（多个）**：累计成本价、累计回本 FDV
> - **全体买家**：每分钟消耗的 VIRTUAL 总量汇总（分钟级时间序列）
>
> 注意：本程序不签名、不发交易、不做 approve，纯监控与计算。

---

## 0) 关键概念（你要的口径）

### 0.1 地址类型（必须可手动输入）
每个“打新代币/内盘”需要你手动输入这三类地址：
- `INTERNAL_POOL_ADDR`：内盘合约地址（你在 BaseScan 看到的“内盘地址底下的交易”那个 0x…）
- `FEE_ADDR`：**手续费地址**（你验证能看到“买家 -> 手续费地址”的 VIRTUAL 转账的那个地址）
- `TAX_ADDR`：**税收地址**（与手续费分开；用于识别税收那笔 VIRTUAL 转账）

> 备注：本程序默认“手续费固定 1%”，税收不参与反推总花费（除非你额外想统计税收流量，见后文）。

### 0.2 你定义的“总花费 VIRTUAL”
你当前的核心逻辑（按你的口径）：
- `feeV` = 买家在该 tx 中转给 `FEE_ADDR` 的 VIRTUAL 数量
- `spentV_est = feeV / 0.01`（即 fee * 100）作为该笔“总消耗 VIRTUAL”（总花费）

> 税收（TAX）是另外一笔转账，单独统计，不影响 `spentV_est` 的反推口径。

### 0.3 你关心的输出指标
- **单笔成本价（V/Token）**：`costV = spentV_est / tokenBought`
- **有效回本 FDV（V 计价）**：`breakevenFDV_V = costV * TOTAL_SUPPLY`
- **有效回本 FDV（USD 计价）**：`breakevenFDV_USD = breakevenFDV_V * virtualPriceUSD`
- **指定钱包累计成本**：多笔累计 `sumSpentV_est / sumTokenBought`

---

## 1) 运行方式（必须具备的输入位置）

### 1.1 手动输入配置（CLI 或 config.json）
程序必须提供**明确的手动输入**入口（CLI 参数或 config 文件），至少包含：

#### A. 链与 RPC
- `CHAIN_ID = 8453`（Base）
- `WS_RPC_URL`（WebSocket，实时订阅 logs）
- `HTTP_RPC_URL`（HTTP，补拉 receipt、读 decimals、读 reserves）

#### B. 打新/内盘项目配置（支持多个同时跑）
- `LAUNCH_CONFIGS`: 数组，每个元素：
  - `name`: 项目名（任意）
  - `internal_pool_addr`: 内盘地址
  - `fee_addr`: 手续费地址
  - `tax_addr`: 税收地址
  - `token_total_supply`: 默认 `1_000_000_000`（可覆盖）
  - `fee_rate`: 默认 `0.01`（可覆盖）

> 为什么要支持多个：因为你说“多个代币发行时间会重合”，你要同时监控多个内盘地址。

#### C. 我的钱包（多个）
- `MY_WALLETS`: 数组（多个地址），用于输出“我的钱包成本价/回本 FDV”
  - 例如：`["0xaaa...", "0xbbb...", ...]`

#### D. 输出与统计参数
- `TOP_N`（大户榜，可选，默认 50）
- `AGG_MINUTE_WINDOW`（每分钟汇总；默认一直累计，也可设只保留最近 N 分钟）
- `CONFIRMATIONS`（默认 1）
- `LOG_LEVEL`

---

## 2) 数据获取：必须实时（Real-time）

### 2.1 监听机制（必须用 WebSocket logs）
使用 `eth_subscribe` 订阅 ERC20 `Transfer` 事件（topic0 = Transfer(address,address,uint256)）。
机器人**不能**靠“每秒刷新网页”，必须用链上实时事件。

### 2.2 触发条件（以 txHash 为核心）
任何监听到的相关 log，都将 `txHash` 放入队列（去重），然后用 HTTP RPC 拉：
- `eth_getTransactionReceipt(txHash)`

**相关 log 的判定：**
对每个 `LAUNCH_CONFIG`，只要 receipt.logs 中出现以下任一模式，就认为该 tx 属于这个内盘：
- 目标 1：`VIRTUAL.Transfer(to = internal_pool_addr)` 或 `VIRTUAL.Transfer(from = internal_pool_addr)`
- 目标 2：`VIRTUAL.Transfer(to = fee_addr)` 或 `VIRTUAL.Transfer(to = tax_addr)`
- 目标 3：存在某个 ERC20 `Transfer(from = internal_pool_addr)`（内盘发币）或 `Transfer(to = internal_pool_addr)`（可能回收/其它）

> 由于你不想靠“时间猜”，一定要**用 receipt 级别**归因。

---

## 3) 单笔交易解析（同 txHash 绑定）

对某个 `LAUNCH_CONFIG` 与某个 `txHash`：

### 3.1 找“买家地址 buyer”
在 receipt.logs 中：
- buyer 是满足下面条件的地址（按地址分组）：
  1) buyer 有 `VIRTUAL.Transfer(from = buyer, to = fee_addr)` 或 `to = tax_addr` 或 `to = internal_pool_addr`
  2) 同一 txHash 内存在某个 token 的 `Transfer(from = internal_pool_addr, to = buyer, amount>0)`（内盘发币给 buyer）

> 同一 txHash 可能出现多个 buyer（极少但要支持），需要按 buyer 分组输出。

### 3.2 找“买到的代币数量 tokenBought”
对每个 buyer：
- 在 receipt.logs 中找到所有 `Transfer(from = internal_pool_addr, to = buyer)` 的 ERC20 log
- `tokenBought` = 对同一种 token 的 amount 求和
- `agentTokenAddr` = 这些 log 的 `address`（即 token 合约地址）

> 若同 txHash 内 buyer 收到多种 token（异常），按 token 合约分别输出一条记录。

### 3.3 找“手续费 feeV（固定 1%）”
对每个 buyer：
- `feeV` = receipt.logs 中所有 `VIRTUAL.Transfer(from = buyer, to = fee_addr)` 求和

### 3.4 找“税收 taxV”（与手续费分开）
对每个 buyer：
- `taxV` = receipt.logs 中所有 `VIRTUAL.Transfer(from = buyer, to = tax_addr)` 求和

### 3.5 反推“总花费 VIRTUAL”
对每个 buyer（按你的口径）：
- `spentV_est = feeV / fee_rate`（默认 fee * 100）
- 税收 `taxV` 仅用于展示/统计，不参与 `spentV_est` 计算（除非你以后改口径）

> 可选增强（推荐但非强制）：校验 fallback  
> `spentV_actual = buyer 在该 tx 中 VIRTUAL 净流出`  
> 如果 `spentV_actual` 与 `spentV_est` 偏差 > 2%，标记异常并同时输出两者。

### 3.6 计算成本、回本 FDV
对每个 buyer & token：
- `costV = spentV_est / tokenBought`
- `breakevenFDV_V = costV * token_total_supply`
- 获取 `virtualPriceUSD`（见第 4 节）
- `breakevenFDV_USD = breakevenFDV_V * virtualPriceUSD`

---

## 4) VIRTUAL 单价（virtualPriceUSD）

必须提供一种实时/近实时的 VIRTUAL USD 价格输入方式：

### 4.1 推荐：链上价格（onchain）
- 配置 `VIRTUAL_USDC_PAIR_ADDR`（或 VIRTUAL/WETH + WETH/USD 组合）
- 每个新区块更新一次缓存
- 用 UniswapV2 Pair `getReserves()` 计算价格（按 token0/token1 判断方向）

### 4.2 备用：外部 API（可选）
- 支持 Coingecko/CMC 等
- 做缓存与限速（例如 10 秒更新一次）

---

## 5) 统计模块（你要的实时持续统计）

### 5.1 我的钱包（多个）累计成本
对每个 `LAUNCH_CONFIG` & `agentTokenAddr` & `wallet in MY_WALLETS`：
- `sumFeeV += feeV`
- `sumSpentV_est += spentV_est`
- `sumTokenBought += tokenBought`
- `avgCostV = sumSpentV_est / sumTokenBought`
- `breakevenFDV_V = avgCostV * total_supply`
- `breakevenFDV_USD = breakevenFDV_V * virtualPriceUSD`

> 输出时必须能按 “钱包 → 项目/代币 → 当前累计成本/回本FDV” 查询。

### 5.2 全体买家：每分钟消耗 VIRTUAL 汇总（必须实现）
对每个 `LAUNCH_CONFIG`（也可按 tokenAddr 细分）：
- 按交易时间戳 `ts` 归入分钟桶 `minuteKey = floor(ts / 60) * 60`
- `minuteSpentV[minuteKey] += spentV_est`（汇总所有买家的总消耗）
- 同时可选记录：
  - `minuteFeeV += feeV`
  - `minuteTaxV += taxV`
  - `minuteBuyCount += 1`
  - `minuteUniqueBuyers`（去重 buyer 数）

> 要求：实时更新（事件驱动），延迟不超过 1-2 个区块。

### 5.3 大户榜（可选但建议）
“开盘一开始没有大户”的解决方案：
- 实时累计每个地址的 `sumSpentV_est` 与 `sumTokenBought`
- 按 `sumSpentV_est` 排序取前 `TOP_N`，即当前大户榜

---

## 6) 输出形式（必须易于二次开发）

### 6.1 单笔事件输出（建议 JSONL）
每解析出一条 buy 记录（buyer & token），输出一条 JSON：
```json
{
  "project": "WORK",
  "txHash": "0x...",
  "blockNumber": 0,
  "timestamp": 0,
  "internalPool": "0x...",
  "feeAddr": "0x...",
  "taxAddr": "0x...",
  "buyer": "0x...",
  "tokenAddr": "0x...",
  "tokenBought": "123.45",
  "feeV": "0.50",
  "taxV": "5.00",
  "spentV_est": "50.00",
  "costV": "0.405",
  "totalSupply": 1000000000,
  "breakevenFDV_V": "405000000",
  "virtualPriceUSD": "0.58",
  "breakevenFDV_USD": "234900000",
  "isMyWallet": true
}
6.2 查询接口（可选：HTTP）

GET /mywallets：我的钱包列表与累计统计

GET /mywallets/:addr：某钱包明细（按项目/代币）

GET /minutes?project=...&from=...&to=...：分钟级 spentV 汇总

GET /leaderboard?project=...&top=N

7) 存储与容错（建议 SQLite）

建议落库（便于重启续跑与查询）：

events：单笔 buy 事件表（txHash+buyer+tokenAddr 唯一）

wallet_positions：我的钱包累计

minute_agg：分钟桶汇总

leaderboard_cache：可选缓存

容错要求：

txHash 去重

RPC 失败重试（指数退避）

重启后从 lastProcessedBlock 续跑（或从 DB 的最新 block 续跑）

8) 验收标准（Acceptance Criteria）

能手动输入：

内盘地址、税收地址、手续费地址（支持多个项目）

我的钱包地址列表（多个）

能实时（WebSocket）解析出单笔买入的：

buyer、tokenBought、feeV、taxV、spentV_est、costV、回本 FDV

能实时维护：

我的钱包累计成本/回本 FDV（多钱包）

每分钟全体买家 spentV_est 汇总（分钟级时间序列）

多币同发不串数据：必须按 txHash+receipt.logs 归因到对应内盘配置与 tokenAddr

9) 安全声明

本程序不持有私钥、不签名、不发交易

仅用于链上数据分析与监控，不构成投资建议



GPT问：如果你愿意，我也可以把这个 MD 再补一段“推荐的 config.json 模板”（不含任何代码逻辑，只是配置示例），你直接复制到 Codex 里就能让它按模板做解析与存储。
::contentReference[oaicite:0]{index=0}

GPT回答：{
  "CHAIN_ID": 8453,
  "WS_RPC_URL": "wss://YOUR_BASE_WS_RPC",
  "HTTP_RPC_URL": "https://YOUR_BASE_HTTP_RPC",

  "VIRTUAL_TOKEN_ADDR": "0x0b3e328455c4059EEb9e3f84b5543F74E24e7E1b",
  "FEE_RATE_DEFAULT": 0.01,
  "TOTAL_SUPPLY_DEFAULT": 1000000000,

  "LAUNCH_CONFIGS": [
    {
      "name": "WORK",
      "internal_pool_addr": "0xINTERNAL_POOL_ADDR",
      "fee_addr": "0xFEE_ADDR",
      "tax_addr": "0xTAX_ADDR",
      "token_total_supply": 1000000000,
      "fee_rate": 0.01
    }
  ],

  "MY_WALLETS": [
    "0xYOUR_WALLET_1",
    "0xYOUR_WALLET_2"
  ],

  "TOP_N": 50,
  "CONFIRMATIONS": 1,
  "AGG_MINUTE_WINDOW": 0,

  "PRICE_MODE": "onchain_pool",
  "VIRTUAL_USDC_PAIR_ADDR": "0xVIRTUAL_USDC_PAIR_ADDR",

  "LOG_LEVEL": "info"
}




