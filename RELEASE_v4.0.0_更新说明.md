# V4.0.0 更新说明

## 版本定位
V4.0.0 是在 V3.1.0 基础上的「监控链路与延迟口径升级版」。
核心目标是两点：
1. 无人看盘时自动降请求，降低云端节点消耗。
2. 延迟指标改为更接近实战观测的口径，减少误读与抖动。

## 主要更新
### 1) 新增无人值守自动休眠（心跳控制）
- 新增配置项：
  - `AUTO_IDLE_PAUSE`（默认示例为 `true`）
  - `UI_HEARTBEAT_TIMEOUT_SEC`（默认示例为 `20`）
- 前端页面打开后会自动发送心跳；关闭页面后超时无心跳，后端进入空闲暂停。
- 暂停范围：
  - realtime WS 监听
  - backfill 自动回扫循环
  - onchain 价格轮询
- 页面重新打开后自动恢复，不启用自动追平（历史缺口仍由手动回扫处理）。

### 2) 新增心跳接口
- 新增 API：
  - `POST /heartbeat`
  - `GET /heartbeat`（兼容调用）
- writer 将 UI 心跳写入总线，realtime/backfill 读取同一心跳状态做暂停/恢复判断。

### 3) 健康接口增强
- `GET /health` 新增字段：
  - `server_now`
  - `server_now_ms`
  - `autoIdlePause`
  - `idleTimeoutSec`
  - `idlePaused`
  - `uiLastHeartbeatAt`

### 4) 延迟面板口径升级
- 顶部性能条改为独立卡片，避免信息拥挤。
- `云端->界面` 与 `端到端` 口径调整为“首次显示统计”：
  - 仅在交易首次进入当前页面统计集时计入样本，避免“数据年龄”持续变大导致误解。
- 延迟统计改为「按项目隔离」：
  - 解决“不同项目使用同一内盘地址/同一 tx_hash 时，第二个项目无数据”的问题。

### 5) 延迟显示规则优化
- 顶部三项延迟（链上->云端 / 云端->界面 / 端到端）支持单位自适应：
  - `< 1s` 显示 `ms`
  - `>= 1s` 显示 `s`
- 底部“交易录入延迟”表保持秒级数值展示（不显示单位，避免秒级源数据被误解为毫秒精度）。

### 6) 版本标识更新
- 页面标题与页头版本更新为 `v4.0.0`。
- 后端命令行描述更新为 `v4.0.0`。
- `README.md` 更新本版本文档索引与新能力说明。

## 兼容性说明
- 数据库结构无破坏性变更；可直接在原有数据上升级运行。
- 若你的部署是三进程模式，升级后需要重启 `writer/realtime/backfill` 全部进程以生效。

## 配置建议（V4.0.0）
- `AUTO_IDLE_PAUSE: true`
- `UI_HEARTBEAT_TIMEOUT_SEC: 20`
- 若你长期盯盘且不希望自动暂停，可设：
  - `AUTO_IDLE_PAUSE: false`
