# Virtuals 打新/内盘实时扫描机器人需求方案 v1.1（低延迟无 Supabase 版）

## 0. 文档信息
- 文档版本：`v1.1`
- 基线版本：`v1.0（要求.md）`
- 更新日期：`2026-02-12`
- 目标链：`Base (chainId = 8453)`
- 文档定位：`研发执行版需求说明（可直接进入设计与开发）`

---

## 1. 版本升级说明（v1.0 -> v1.1）

### 1.1 本次升级核心原因
你明确提出：`不使用 Supabase`。原因是远程往返读取/写入带来明显延迟，不满足实时监控与分钟级聚合场景的时效要求。

### 1.2 v1.1 的关键变化
1. 移除 Supabase 在热路径中的任何依赖。
2. 存储改为本地优先：`SQLite（默认）`，可选 `本机/同机房 PostgreSQL`。
3. 解析链路采用“`内存聚合 + 异步批量落库`”，避免每条事件同步写库导致抖动。
4. 明确实时目标：事件驱动，端到端延迟控制在 1-2 区块内。
5. 增加可恢复性设计：checkpoint、去重、断线重连、重启续跑。
6. 输出标准化：JSONL + HTTP 查询接口，便于二次开发。

### 1.3 变更对比表
| 维度 | v1.0 | v1.1 |
|---|---|---|
| 实时链路存储 | 可接 Supabase | 禁止 Supabase 热路径，改本地存储 |
| 写入方式 | 未强制 | 异步批量写入（降低写放大） |
| 容错恢复 | 基础描述 | 明确 checkpoint + 幂等去重 + 回放 |
| 性能指标 | 定性 | 给出延迟/吞吐/可用性目标 |
| 验收标准 | 基础项 | 增加可量化验收项 |

---

## 2. 需求理解确认（对齐你的真实诉求）

以下是我对你需求的完整理解，作为开发前确认基准：

1. 你要的是`只读监控系统`，不是交易机器人。
2. 目标是实时抓 Base 链 Virtuals 打新/内盘买入行为。
3. 单笔买入核心口径是：`手续费 feeV` 反推 `spentV_est = feeV / fee_rate`（默认 1%）。
4. 税收 `taxV` 只做展示与统计，不参与 `spentV_est` 反推。
5. 必须支持多个项目并行（多个内盘地址同时监控）。
6. 必须支持多个“我的钱包”地址，并持续维护累计成本与回本 FDV。
7. 需要全体买家分钟级消耗曲线（minute agg）。
8. 归因必须基于 `txHash + receipt.logs`，不能靠时间猜测。
9. 需要低延迟与稳定性，`Supabase 不再使用`。
10. 产物必须可二次开发，数据输出与接口要标准化。

---

## 3. 项目目标与边界

## 3.1 项目目标（Must）
1. 实时监听并识别“内盘买入”相关交易。
2. 对每笔 buy 产出完整结构化记录（buyer、token、feeV、taxV、spentV_est、成本、回本 FDV）。
3. 输出“我的钱包”累计持仓成本与回本 FDV。
4. 输出全体买家分钟级 spentV 汇总时间序列。
5. 系统重启后可续跑，避免重复统计和数据错乱。

## 3.2 非目标（Out of Scope）
1. 不签名、不发交易、不做 approve。
2. 不提供投资建议或自动策略执行。
3. 不做跨链监控（仅 Base）。
4. 不做复杂前端可视化（仅提供 JSONL/API 数据能力）。

---

## 4. 总体架构（v1.1）

## 4.1 架构原则
1. 低延迟优先：热路径不依赖远程数据库。
2. 幂等优先：按 `txHash + buyer + tokenAddr` 唯一约束去重。
3. 可恢复优先：任何时刻可根据 checkpoint 继续处理。
4. 可扩展优先：多项目、多钱包并发不串数据。

## 4.2 组件划分
1. `WS Listener`：订阅 ERC20 Transfer 日志。
2. `Tx Queue`：去重排队 txHash，控制消费速率。
3. `Receipt Fetcher`：HTTP RPC 拉 receipt、block 时间戳。
4. `Parser`：按 launch_config 归因，提取 buyer/token/fee/tax。
5. `Calculator`：计算 spentV_est、costV、breakevenFDV。
6. `Price Service`：提供 virtualPriceUSD（链上优先）。
7. `Aggregator`：维护 my wallets、minute agg、leaderboard。
8. `Storage Writer`：异步批量写 SQLite/PostgreSQL。
9. `HTTP API`：查询统计结果。
10. `State Manager`：checkpoint 与恢复。

## 4.3 数据流（热路径）
1. 监听到相关 log -> 生成 txHash。
2. txHash 入队并去重。
3. 拉 receipt 与 block 时间戳。
4. 对每个 launch_config 执行归因。
5. 解析 buyer + token + feeV + taxV + tokenBought。
6. 计算 spentV_est / costV / breakevenFDV。
7. 更新内存聚合（钱包、分钟桶、大户榜）。
8. 输出 JSONL 事件。
9. 异步批量落库。
10. 更新 checkpoint。

---

## 5. 功能需求（详细）

## 5.1 配置管理
### FR-001 链配置
- 必须支持 `CHAIN_ID=8453`。
- 必须支持 `WS_RPC_URL` 与 `HTTP_RPC_URL` 分离配置。

### FR-002 多项目配置
- `LAUNCH_CONFIGS` 支持多个对象并行监控。
- 每个项目必须包含：
  - `name`
  - `internal_pool_addr`
  - `fee_addr`
  - `tax_addr`
  - `token_total_supply`（默认 1e9）
  - `fee_rate`（默认 0.01）

### FR-003 我的钱包配置
- `MY_WALLETS` 支持多个地址。
- 地址入库前统一小写并校验 `0x` 格式。

### FR-004 性能与行为配置
- 支持 `CONFIRMATIONS`、`TOP_N`、`AGG_MINUTE_WINDOW`、`LOG_LEVEL`。
- 支持 `RECEIPT_WORKERS`、`DB_BATCH_SIZE`、`DB_FLUSH_MS`。

### FR-005 Supabase 禁用约束
- v1.1 禁止依赖 `SUPABASE_URL`、`SUPABASE_KEY`。
- 如检测到相关配置，程序应启动失败并提示“v1.1 不允许 Supabase 热路径”。

## 5.2 实时监听与归因
### FR-006 WebSocket 订阅
- 通过 `eth_subscribe` 监听 `Transfer(address,address,uint256)`。
- 断线自动重连，使用指数退避。

### FR-007 相关交易识别
- 命中以下任意模式即判定为候选 tx：
  1. VIRTUAL 与 `internal_pool_addr` 的转账。
  2. VIRTUAL 转入 `fee_addr` 或 `tax_addr`。
  3. 任意 ERC20 与 `internal_pool_addr` 的转账。

### FR-008 receipt 级归因
- 解析必须基于 `receipt.logs`。
- 同一 tx 可归因到多个 buyer；同 buyer 可归因多个 token。

## 5.3 单笔买入解析
### FR-009 buyer 判定
- buyer 需同时满足：
  1. 在该 tx 内向 `fee_addr`/`tax_addr`/`internal_pool_addr` 转出过 VIRTUAL。
  2. 在该 tx 内收到 `internal_pool_addr -> buyer` 的 token 转账。

### FR-010 tokenBought 计算
- 按 token 合约分组，汇总 `internal_pool_addr -> buyer` 的 amount。

### FR-011 feeV 计算
- 汇总 `VIRTUAL.Transfer(from=buyer,to=fee_addr)`。

### FR-012 taxV 计算
- 汇总 `VIRTUAL.Transfer(from=buyer,to=tax_addr)`。

### FR-013 spentV_est 计算
- `spentV_est = feeV / fee_rate`。
- 当 `fee_rate <= 0` 时，该条记录标记错误并丢弃。

### FR-014 fallback 校验（建议）
- 计算 `spentV_actual = buyer 在该 tx 的 VIRTUAL 净流出`。
- 若 `abs(spentV_actual - spentV_est)/spentV_est > 2%`，标记 `anomaly=true`。

### FR-015 成本与回本计算
- `costV = spentV_est / tokenBought`。
- `breakevenFDV_V = costV * total_supply`。
- `breakevenFDV_USD = breakevenFDV_V * virtualPriceUSD`。

## 5.4 价格服务
### FR-016 链上价格优先
- 支持读取 `VIRTUAL_USDC_PAIR_ADDR` 的 reserves。
- 每个新区块或固定秒级刷新缓存。

### FR-017 备用价格源
- 可选外部 API（Coingecko/CMC），但必须有缓存与限速。

### FR-018 价格容错
- 价格短时不可用时：
  - `virtualPriceUSD` 返回最近一次缓存值并标记 `isPriceStale=true`。
  - 超时阈值（如 120 秒）后可输出 `null` 并告警。

## 5.5 统计模块
### FR-019 我的钱包累计
- 维度：`project + token + wallet`。
- 指标：`sumFeeV`、`sumSpentV_est`、`sumTokenBought`、`avgCostV`、回本 FDV（V/USD）。

### FR-020 分钟级聚合
- 维度：`project + minuteKey`（可扩到 token）。
- 指标：`minuteSpentV`、`minuteFeeV`、`minuteTaxV`、`minuteBuyCount`、`minuteUniqueBuyers`。

### FR-021 大户榜
- 维度：`project + buyer`。
- 指标：`sumSpentV_est`、`sumTokenBought`。
- 排序：按 `sumSpentV_est desc`，取前 `TOP_N`。

## 5.6 输出与查询
### FR-022 JSONL 事件输出
- 每个 `buyer + token` 输出一条记录。
- 字段必须完整且类型稳定（字符串数值统一格式）。

### FR-023 HTTP 查询接口（可选但建议）
- `GET /health`
- `GET /mywallets`
- `GET /mywallets/:addr`
- `GET /minutes?project=...&from=...&to=...`
- `GET /leaderboard?project=...&top=N`

### FR-024 查询一致性
- API 可读到“已解析并已提交聚合”的数据。
- 允许与最新链上事件有亚秒到数秒级延迟，不得倒退。

## 5.7 存储与容错
### FR-025 默认存储
- 默认 `SQLite` 文件库。
- 单实例场景必须稳定运行。

### FR-026 可选存储
- 可切换到本地/同机房 PostgreSQL（非必需）。
- 严禁把 Supabase 放入实时热路径。

### FR-027 幂等去重
- `events` 表以 `txHash+buyer+tokenAddr+project` 唯一约束。
- 重放时不得产生重复记录。

### FR-028 checkpoint
- 持久化 `lastProcessedBlock` 与处理时间。
- 重启后按 checkpoint 续跑。

### FR-029 回补机制
- 如果 WS 断线，按 `fromBlock -> toBlock` 回补日志。
- 回补与实时流统一走同一解析流程，确保一致性。

### FR-030 重试机制
- RPC 失败重试（指数退避，最大重试次数可配置）。
- 超出重试阈值进入死信队列并告警。

---

## 6. 数据模型（v1.1 建议）

## 6.1 表：events（单笔买入事件）
- 主键：自增 id
- 唯一键：`project, tx_hash, buyer, token_addr`
- 关键字段：
  - `project`
  - `tx_hash`
  - `block_number`
  - `block_timestamp`
  - `internal_pool`
  - `fee_addr`
  - `tax_addr`
  - `buyer`
  - `token_addr`
  - `token_bought`
  - `fee_v`
  - `tax_v`
  - `spent_v_est`
  - `spent_v_actual`（可空）
  - `cost_v`
  - `total_supply`
  - `virtual_price_usd`
  - `breakeven_fdv_v`
  - `breakeven_fdv_usd`
  - `is_my_wallet`
  - `anomaly`
  - `created_at`

## 6.2 表：wallet_positions（我的钱包累计）
- 唯一键：`project, wallet, token_addr`
- 字段：
  - `sum_fee_v`
  - `sum_spent_v_est`
  - `sum_token_bought`
  - `avg_cost_v`
  - `breakeven_fdv_v`
  - `breakeven_fdv_usd`
  - `updated_at`

## 6.3 表：minute_agg（分钟桶）
- 唯一键：`project, minute_key`
- 字段：
  - `minute_spent_v`
  - `minute_fee_v`
  - `minute_tax_v`
  - `minute_buy_count`
  - `minute_unique_buyers`
  - `updated_at`

## 6.4 表：leaderboard
- 唯一键：`project, buyer`
- 字段：
  - `sum_spent_v_est`
  - `sum_token_bought`
  - `last_tx_time`

## 6.5 表：system_state
- 主键：`key`
- 字段：
  - `value`
  - `updated_at`
- 示例：
  - `last_processed_block`
  - `last_processed_tx_time`
  - `ws_last_reconnect_time`

## 6.6 表：dead_letters（可选）
- 用于记录反复失败的 tx 及错误原因，便于排查。

---

## 7. 接口规范（建议）

## 7.1 事件 JSONL 结构（标准化）
```json
{
  "project": "WORK",
  "txHash": "0x...",
  "blockNumber": 12345678,
  "timestamp": 1700000000,
  "internalPool": "0x...",
  "feeAddr": "0x...",
  "taxAddr": "0x...",
  "buyer": "0x...",
  "tokenAddr": "0x...",
  "tokenBought": "123.450000",
  "feeV": "0.500000",
  "taxV": "5.000000",
  "spentV_est": "50.000000",
  "spentV_actual": "49.850000",
  "costV": "0.405023",
  "totalSupply": "1000000000",
  "breakevenFDV_V": "405023000.000000",
  "virtualPriceUSD": "0.580000",
  "breakevenFDV_USD": "234913340.000000",
  "isMyWallet": true,
  "anomaly": false,
  "isPriceStale": false
}
```

## 7.2 HTTP API（建议）
### GET /health
- 返回服务状态、最新区块、队列长度、延迟指标。

### GET /mywallets
- 返回所有监控钱包及累计统计。

### GET /mywallets/:addr
- 返回指定钱包按项目/代币维度明细。

### GET /minutes
- 参数：
  - `project` 必填
  - `from`/`to` 秒级时间戳
- 返回分钟桶序列。

### GET /leaderboard
- 参数：`project`、`top`
- 返回按 `sumSpentV_est` 排序结果。

---

## 8. 非功能需求（可量化）

## 8.1 延迟
1. 监听到相关 tx 后，`P95` 解析完成延迟 <= `6 秒`。
2. 常态链路目标为 `1-2 区块`内完成聚合更新。

## 8.2 吞吐
1. 支持短时突发 `>= 300 tx/min` 不丢数据。
2. 写库采用批量提交，单批建议 `50-500` 条可配置。

## 8.3 可用性
1. WS 断线自动恢复，恢复后自动回补缺失区间。
2. 24h 运行期间无人工干预崩溃（目标）。

## 8.4 数据正确性
1. 幂等去重误差为 0（同一事件不可重复累计）。
2. 所有金额字段采用高精度十进制计算，不得用 float 直接累计。

## 8.5 可观测性
1. 指标：队列积压、解析耗时、RPC 错误率、重试次数、写库耗时。
2. 日志分级：`debug/info/warn/error`。
3. 关键异常需单独计数并可查询。

---

## 9. 配置规范（v1.1）

```json
{
  "CHAIN_ID": 8453,
  "WS_RPC_URL": "wss://YOUR_BASE_WS_RPC",
  "HTTP_RPC_URL": "https://YOUR_BASE_HTTP_RPC",

  "VIRTUAL_TOKEN_ADDR": "0x0b3e328455c4059EEb9e3f84b5543F74E24e7E1b",
  "FEE_RATE_DEFAULT": 0.01,
  "TOTAL_SUPPLY_DEFAULT": 1000000000,

  "LAUNCH_CONFIGS": [
    {
      "name": "WORK",
      "internal_pool_addr": "0xINTERNAL_POOL_ADDR",
      "fee_addr": "0xFEE_ADDR",
      "tax_addr": "0xTAX_ADDR",
      "token_total_supply": 1000000000,
      "fee_rate": 0.01
    }
  ],

  "MY_WALLETS": [
    "0xYOUR_WALLET_1",
    "0xYOUR_WALLET_2"
  ],

  "PRICE_MODE": "onchain_pool",
  "VIRTUAL_USDC_PAIR_ADDR": "0xVIRTUAL_USDC_PAIR_ADDR",
  "PRICE_REFRESH_SEC": 3,

  "DB_MODE": "sqlite",
  "SQLITE_PATH": "./data/virtuals_v11.db",
  "DB_BATCH_SIZE": 200,
  "DB_FLUSH_MS": 500,

  "RECEIPT_WORKERS": 8,
  "CONFIRMATIONS": 1,
  "TOP_N": 50,
  "AGG_MINUTE_WINDOW": 0,
  "MAX_RPC_RETRIES": 5,
  "LOG_LEVEL": "info"
}
```

### 9.1 配置校验规则
1. 地址字段必须为合法 20 字节地址。
2. `fee_rate` 必须在 `(0,1)`。
3. `DB_MODE` 仅允许 `sqlite` 或 `postgres`。
4. 若检测到 Supabase 配置项，启动失败并报错。

---

## 10. 验收标准（v1.1）

## 10.1 功能验收
1. 可手动配置多个项目地址（内盘、手续费、税收）。
2. 可手动配置多个我的钱包地址。
3. 实时解析单笔 buy 输出字段完整，公式正确。
4. 能稳定维护钱包累计指标与分钟级聚合。
5. 多项目并发时，事件归因不串项目、不串 token。

## 10.2 性能验收
1. 连续运行 2 小时，不丢事件。
2. 突发交易压测下无明显积压失控（队列可回落）。
3. `P95` 解析延迟 <= 6 秒。

## 10.3 容错验收
1. 人为断开 WS 后，系统自动重连并回补缺口。
2. 人为重启服务后，从 checkpoint 正确续跑。
3. 同一 tx 重放不会重复入库或重复累计。

## 10.4 数据验收
1. 手工抽样核对 20 笔交易，`feeV/taxV/tokenBought/spentV_est` 全量一致。
2. 分钟桶聚合总和与原始事件求和一致（允许精度微差，误差 < 1e-12）。

---

## 11. 风险与对策
1. RPC 不稳定：多节点容灾 + 重试 + 超时治理。
2. 合约行为变化：解析逻辑保留规则扩展点，支持版本化配置。
3. 价格源中断：缓存回退并标记 stale。
4. 高峰写入压力：批量写 + 索引优化 + 限流回压。
5. 地址配置错误：启动前校验 + 启动后健康检查告警。

---

## 12. 实施里程碑（建议）
1. M1（0.5 天）：配置层 + WS/HTTP 基础链路 + checkpoint 框架。
2. M2（1 天）：receipt 解析 + 单笔事件计算 + JSONL 输出。
3. M3（1 天）：钱包聚合 + 分钟桶 + 大户榜 + SQLite 落库。
4. M4（0.5 天）：HTTP 查询接口 + 健康检查 + 指标日志。
5. M5（0.5 天）：容错压测 + 回放测试 + 验收报告。

---

## 13. 开发完成定义（Definition of Done）
1. 功能、性能、容错、数据验收四类标准全部通过。
2. 无 Supabase 依赖，热路径本地闭环。
3. 代码可通过配置直接切换多项目并行运行。
4. 输出文档包含配置说明、接口说明、运维说明。

---

## 14. 最终结论（v1.1 方向确认）
本 v1.1 方案已经按你的核心诉求重构：`去 Supabase、强调低延迟、以 receipt 归因为核心、支持多项目多钱包并发、可恢复可扩展`。  
该文档可直接作为下一步系统设计与开发任务拆解依据。

